# Agentic review baseline

Findings from `INSTRUCTION_SET_REVIEW_INSTRUCTIONS.md` judged deliberate rather than
defective. One bullet per finding: the id, then the reason and the date it was reviewed.

Rules (see that file, §7): the reason is never optional; suppression is per finding, never per
check; and a false positive is never baselined — the check definition gets fixed instead.

<!-- Format:
- `RD4:AGENTS_TEMPLATE:constraint-obligations` — Intentional. <why>. Reviewed YYYY-MM-DD.
-->

_No suppressions yet._
